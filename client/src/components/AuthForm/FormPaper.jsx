import React from "react";
import { Box } from "@mui/material";

const FormBox = () => {
  <Box
    sx={{
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      minHeight: "100vh",
    }}
  ></Box>;
};

export default FormBox;
